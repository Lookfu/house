server:
  port: 8080

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml #the location of mapper
  config-location: classpath:config/mybatis-config.xml

domain:
  name: 127.0.0.1:8080

spring:
  mail:
    host: smtp.qq.com
    username: 1668493630@qq.com
    ## not password
    password: weylfefnxipgbacf
    default-encoding: utf-8
  datasource:
    name: druidDataSource
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost:3306/houses?useUnicode=true&characterEncoding=utf-8&useSSL=true&serverTimezone=UTC
      username: root
      password: 123456
      max-active: 100
      initial-size: 1
      max-wait: 60000
      min-idle: 1
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: select 'x'
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-open-prepared-statements: 50
      max-pool-prepared-statement-per-connection-size: 20
