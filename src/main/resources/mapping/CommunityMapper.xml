<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.house.mapper.CommunityMapper">
    <select id="getCommunityName" resultType="string" >
        select name from community where city_id=#{cityId} and community_id=#{communityId};
    </select>

    <select id="getCommunityId" resultType="int">
        select community_id from community where city_id=#{cityId} and name=#{name};
    </select>
</mapper>